# requirements-api.txt — SLIM + PINNED build-riippuvuudet Render-API:lle (goaliq-api).
#
# MIKSI (19.7.2026): render.yaml buildCommand asensi ENNEN koko requirements.txt:n
# (streamlit, jupyterlab, ipywidgets, lightgbm, optuna, mplsoccer, seaborn,
# statsbombpy, cloudscraper, soccerdata…), kaikki pinnaamattomina `>=`. API EI käytä
# niistä yhtäkään ajossa — mutta yksikin niiden (tai transitiivinen) uusi py3.11-
# yhteensopimaton julkaisu kaatoi `render-daily-deploy`-hookin pakottaman buildin →
# Renderin "build failed" -meilit. Tuotanto ei kaatunut (vanha deploy jäi eloon), mutta
# tuoreet FPL-projektiot eivät deployautuneet → /api/fantasy tarjoili vanhentunutta dataa.
#
# MITÄ API OIKEASTI TARVITSEE (todennettu importeista: api/ + api:n lataamat src-moduulit
# src.models.dixon_coles / src.data.loader / src.data.international_results):
#   web-kehys + palvelin + validointi + maksut + HTTP:  fastapi, uvicorn[standard],
#     pydantic, stripe, requests
#   malli + datankäsittely (Dixon-Coles):               pandas, numpy, scipy
# EI sklearnia/plotlya/matplotlibia/statsmodelsia/lightgbmia — ne ovat treeni-/viz-
# moduuleissa joita API-importketju ei lataa.
#
# PINNAUS: koko puu lukittu (lockfile-tyyli) reprodusoituvuuden vuoksi — upstream-julkaisu
# ei voi enää yllättäen kaataa buildia. Versiot resolvoitu cp311-linux-wheeleille
# (pip --dry-run --python-version 3.11 --only-binary=:all:) → ei source-kääntöä Renderissä.
# Majorit pidetty testatuissa rajoissa (pandas 2.x, scipy 1.x, fastapi 0.x, pydantic 2.x);
# vapaa resolve olisi nostanut pandas 3.0 -majorin (ajonaikainen regressioriski).
#
# YLLÄPITO: kun API tarvitsee uuden ajonaikaisen dep:n, lisää se tänne (ei requirements.txt:hen,
# joka jää lokaalille dev/notebook/treeni-ympäristölle). Versiopäivitys = resolvoi uudelleen
# yllä olevalla pip-komennolla ja päivitä pinnit.

# --- Web / API ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
stripe==15.3.1
requests==2.34.2

# --- Malli + data (Dixon-Coles: dixon_coles.py) ---
pandas==2.3.3
numpy==2.4.6
scipy==1.17.1

# --- Transitiiviset (lukittu; resolvoitu yhdessä yllä olevien kanssa cp311-linuxille) ---
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
certifi==2026.6.17
charset-normalizer==3.4.9
click==8.4.2
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic-core==2.46.4
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
pytz==2026.2
PyYAML==6.0.3
six==1.17.0
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1
